
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rizqdaan.admin',
  appName: 'RizqDaan Admin',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
